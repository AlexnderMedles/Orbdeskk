# 🚀 Инструкция по деплою OrbDesk (GitHub + Railway)

Эта инструкция поможет тебе правильно сохранить проект на GitHub и запустить его на Railway, чтобы все работало стабильно.

---

## Часть 1: Загрузка на GitHub

### Как «перезаписать» старый репозиторий новым кодом:
Если у тебя уже есть репозиторий на GitHub и ты хочешь полностью заменить его содержимое этим новым проектом:

1. Открой терминал в папке `OrbDesk`.
2. Выполни эти команды по очереди:

```powershell
# 1. Если гит еще не инициализирован
git init

# 2. Привязываем старый репозиторий (замени ссылку на СВОЮ старую!)
# Если уже привязан, просто пропусти этот шаг
git remote add origin https://github.com/ТВОЕ_ИМЯ/OLD_OrbDesk.git

# 3. Добавляем все новые файлы проекта
git add .

# 4. Делаем коммит
git commit -m "Overwrite with new OrbDesk version"

# 5. ПРИНУДИТЕЛЬНО отправляем код (заменит всё старое на новое)
git push -u origin main --force
```

> [!CAUTION]
> Команда `--force` полностью удалит старую историю и файлы в репозитории на GitHub и заменит их текущими. Используй это только если старый код тебе больше не нужен!

---

> [!TIP]
> Если ты не хочешь пользоваться терминалом, можешь просто перетащить файлы в браузер на странице создания репозитория GitHub.

---

## Часть 2: Деплой на Railway

1. Зайди на [Railway.app](https://railway.app/).
2. Нажми **+ New Project** -> **Deploy from GitHub repo**.
3. Выбери свой репозиторий **OrbDesk**.
4. Нажми **Deploy Now**.

### Проверка настроек:
Railway автоматически подхватит файл `railway.json` и `Procfile`, которые я подготовил. 
- **Start Command**: `uvicorn main:app --host 0.0.0.0 --port $PORT`
- **Port**: Railway сам назначит порт через переменную `$PORT`.

---

## Часть 3: Важный шаг — Привязка Агента

Как только Railway выдаст тебе домен (например, `orbdesk-production.up.railway.app`), тебе нужно обновить его в файле агента, чтобы он знал, куда подключаться.

1. Открой файл `orbdesk_host.py`.
2. Найди строку **38** (или около того):
   ```python
   HUB_URL = "https://твое-приложение.up.railway.app"
   ```
3. Замени адрес на свой актуальный домен от Railway.
4. **Всё!** Теперь ты можешь скачивать этот файл, передавать друзьям, и они смогут подключаться к твоему ПК через браузер.

---

## FAQ (Частые вопросы)

**В: Почему не грузится экран?**
О: Убедись, что на компьютере, где запущен агент, установлены библиотеки:
`pip install mss pyautogui Pillow websockets`

**В: Как выгнать зрителя?**
О: В терминале, где запущен агент, просто нажми клавишу `K`.

**В: Как остановить сессию?**
О: Нажми `Q` в терминале агента — он автоматически отпустит все нажатые кнопки и закроет соединение.
